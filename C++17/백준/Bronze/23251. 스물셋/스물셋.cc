#include<iostream>
using namespace std;
long long t,k;
int main(){
      ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
      cin>>t;
      while(t--){
            cin>>k;
            cout<<k*23<<"\n";
      }
}
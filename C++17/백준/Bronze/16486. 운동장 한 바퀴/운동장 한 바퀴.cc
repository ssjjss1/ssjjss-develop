#include<bits/stdc++.h>
using namespace std;
double a,b;
int main(){
      ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
      cin>>a>>b;
      cout<<fixed<<setprecision(20)<<(a*2.0+2.0*b*3.141592);
}